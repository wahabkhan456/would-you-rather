import React from 'react';
import spinner from '../../assets/img/spinner.gif';

const Spinner = () => {
    return (
        <img src={spinner} alt="Loading..." height="200px"/>
    );
}

export default Spinner;